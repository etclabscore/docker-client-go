Docker images for Ethereum Classic go clients.

### Quick start

Multi-geth:

```
$ cd ethoxy/multi-geth
$ docker build -t mg . && docker run -it mg
```

Classic geth:

```
$ cd ethereumclassic/go-ethereum
$ docker build -t cg . && docker run -it cg
```


